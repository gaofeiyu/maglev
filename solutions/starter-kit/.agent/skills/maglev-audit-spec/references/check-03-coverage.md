---
name: 'check-03-coverage'
description: '审计设计与计划的覆盖情况'

# References
nextStepFile: 'END'
targetDir: '{target}'
---

# 审计步骤 3: 计划覆盖率 (Plan Coverage)

**下一步**: 完成

## 目标
验证 `03_plan.md` 中的任务是否足以实施 `02_design.md` 中的设计。

## 规则
- 🛑 **只读**: 不要修改文件。
- 🧩 **模块映射**: 每个主要设计模块必须在此有对应的 Task Group。

## 审计检查清单

### 1. 任务完备性
- [ ] **API 任务**: 是否每个 Endpoint 都有 `[ ] Implement POST /...`?
- [ ] **Schema 任务**: 是否有 `[ ] Create migration for ...`?
- [ ] **前端任务**: 是否有 `[ ] Build Component ...`?

### 2. 未规划的设计 ("Orphaned Design")
- [ ] 扫描 `02` 中的 `### Components` 或 `### API`。
- [ ] 确认它们出现在 `03` 的 Task List 中。

## 输出模式
追加到 **工程审计报告** 并 **定稿**:

```markdown
## 3. 设计-计划覆盖 (Coverage)
| 检查点 (Check) | 状态 (Status) | 建议 (Suggestion) |
| :--- | :--- | :--- |
| **API 任务** | {PASS/FAIL} | {Missing endpoints?} |
| **前端任务** | {PASS/FAIL} | {Missing components?} |

---
**工程审计完成**.
**最终结论 (Verdict)**: {PASS / CONDITIONAL PASS / FAIL}
```
