---
name: maglev-audit-spec
description: 针对 Spec Cluster (01-03) 的工程一致性审计 (Maglev 标准 v2.0)
version: 1.0.0
---

# Maglev Spec 审计技能 (Audit Spec Skill)

**角色**: "主任工程师" (技术守门人)
**目标**: 完整的 Spec 簇 (`00`, `01`, `02`, `03`)

## 目的
在进入实施阶段 (Coding) 之前，验证整个 Spec 簇的内在一致性和技术可行性。此技能确保设计方案 (`02`) 覆盖了所有需求 (`01`)，并且实施计划 (`03`) 是完备的。

## 能力范围
1.  **追溯性 (Traceability)**: 
    *   `02_design.md` 是否解决了 `01_requirements.md` 中的所有关键 FR？
    *   `03_plan.md` 中的任务是否覆盖了所有设计模块？
2.  **完备性 (Completeness)**:
    *   检查是否缺少关键的技术决策 (如 API Schema, DB Schema)。
3.  **一致性 (Consistency)**:
    *   跨文档的术语是否统一 (例如: 01 说 "User", 02 说 "Account")。

## 用法
```bash
/maglev-audit-spec target="specs/feat-login/"
```

## 工作流
1.  **摄入**: 读取整个 `{target}` 目录下的 `00` 到 `03` 文件。
2.  **扫描**: 运行 `audit-spec.workflow.md`。
    *   步骤 A: 簇完整性检查 (Cluster Integrity)
    *   步骤 B: 需求-设计对齐检查 (Req-Design Alignment)
    *   步骤 C: 设计-计划覆盖检查 (Design-Plan Coverage)
3.  **报告**: 输出 `## 🛡️ Maglev 工程审计报告`，重点标记断裂的追溯链。
